# Токен бота от @BotFather
BOT_TOKEN=123456:ABC...
# Telegram id менеджера (узнать: @userinfobot)
MANAGER_TG_ID=123456789
# Порт для приёма заявок с сайта (POST /apply)
WEB_PORT=8081
# Секрет, чтобы сайт/чужие не могли слать заявки (передаётся в X-Api-Key)
WEB_API_KEY=change-me
# Read-only БД основного бота LevelUp (для /roster — список учеников)
# LEVELUP_DATABASE_URL=postgresql://user:pass@host:5432/levelup_bot